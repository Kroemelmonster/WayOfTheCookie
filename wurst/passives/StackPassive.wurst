package StackPassive
import Action
import BoardStack
import ActionCost
import BoardEntity
import ActionType
import Passive
import PassiveType

public abstract class StackPassive extends Passive
    protected BoardStack stack

    construct(BoardEntity stack, PassiveType passiveType)
        super(stack, passiveType)
        if entity.getType() != EntityType.STACK
            Log.error("Tried to add an Stack Passive to an non Stack")
        this.stack = entity.toStack()