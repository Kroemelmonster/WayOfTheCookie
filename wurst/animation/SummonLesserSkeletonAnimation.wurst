package SummonLesserSkeletonAnimation

import CastAnimation
import Abilities
import Animation

import initlater BoardStack
import initlater Cell

public class SummonLesserSkeletonAnimation extends CastAnimation

    construct(BoardStack stack, Cell target, AnimationListener listener)
        super(stack, target, listener)

    override function onCastExecute()
        flashEffect(Abilities.raiseSkeleton, target.getCoord())
    